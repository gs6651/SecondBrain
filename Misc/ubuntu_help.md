
- gsettings set org.gnome.SessionManager logout-prompt true     # To disable timer for shutdown / reboot
- sudo nano /etc/bluetooth/main.conf        # To disable auto ON bluetooth 'AutoEnable=false'

